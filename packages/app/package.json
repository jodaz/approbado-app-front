{
  "name": "@approbado/app",
  "version": "0.1.0",
  "description": "Users webapp",
  "private": true,
  "scripts": {
    "start": "react-app-rewired vite --port 3000",
    "build": "react-app-rewired vite build",
    "serve": "react-app-rewired vite preview"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
